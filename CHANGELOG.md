# 更新记录

## 独立版 r16 · 2026-09-20

- 修复压缩只沿用主票出口、却未携带主票的问题：注入开启且已有可用主票时，V1/V2 压缩同时携带票并绑定来源节点；仍不等待采集、不套用生成响应 state 检查。会话及历史显示“已带主票 / 未注入主票”，并区分压缩响应。

- “随机换节点采集一次”明确改为“手动打票 · 随机节点一次”：每次点击只尝试一个节点，不受批间冷却、失败退避、备用错峰、小时预算或暂停倒计时限制。
- 手动打票不占用或重置自动采集预算与轮询进度；自动采集仍遵守原来的计时和暂停规则。优先换到不同的可用节点，只有一个备选节点时可立即再试。
- 保留主备满额、登录/权限异常、正在采集和节点停用/失败等非计时检查，以及请求的网络超时；已有主卡与来源绑定不变。

## 独立版 r15 · 2026-09-20

- 会话标题下显示最近正式回复或压缩回复中上游声明的模型名，与请求模型不同则标注“模型不一致”；未返回可识别模型时明确显示，避免沿用旧值。后台采集和模型列表不会覆盖此记录。
- 支持 Responses SSE 生命周期事件中的 `response.model` 和 JSON Response 的 `model`，沿用现有 gzip/zstd 解码与有界观察；仅记录模型元数据，不记录回答正文、不改变 state 绑定和请求重放规则。
- 每次请求历史和本地日志同时记录请求模型与响应模型。该字段表示上游声明，不构成模型质量或底层路由的独立证明。

## 独立版 r13 · 2026-09-20

- 网络超时、连接失败及普通 5xx 不再删除主备 state；自动采集失败节点仍停用，已有卡保持来源绑定，手动停用节点仍阻止请求。重启可恢复网络失败节点已有卡。
- 按最新要求，恢复无主用也等待默认 180 秒批次间隔；新增“随机换节点采集一次”，跳过本地间隔但遵守小时预算和上游限制，不改变固定设置或自动轮询进度。
- 普通回复首次收牌支持 gzip/zstd 与实际 SSE 完成判定，以及完成的 JSON Response；不只看响应头长度，未完成/失败仍不入库，页面解释未入库原因。

## 独立版 r12 · 2026-09-20

- 无可用主 state 时，跳过批次 180 秒冷却和旧的内存等待时间，继续当前随机轮次剩余节点；首张取得后结束本批，等待备用补采间隔。
- 主用失效或到期且无备用时立即恢复寻找；仍有备用可提升时保留备用补采节奏。恢复旧预算文件不再把无主用会话卡在批间冷却。
- 调度、手动采集、严格模式 Retry-After 和页面采用同一等待规则；每小时预算、上游暂停、节点暂停/失败停用、空闲停止与逐次退避模式保持生效。
- 连续采集仅在有实际完成的探测时立即调度下一批；剩余节点全部暂停时等待定时检查，避免空转。

## 独立版 r11 · 2026-09-20

- 采集节点每轮随机排序，各账号/模型分别记录已尝试名单；跨批次、采集成功、退避、空闲和重启继续剩余节点，全部可用节点完成后才开启下一轮。
- 固定节点和手动指定节点继续按用户选择执行，不重置自动轮询进度；未访问的临时暂停节点等待恢复，失败/停用节点自动排除。
- 保留每批最多 6 次、批间 180 秒、每小时总预算与空闲停止规则；页面新增当前轮次及完成数量，区别“采集批次”和“完整节点轮询”。
- 本机订阅按实际 Google HTTPS 连通和首页内容筛选，排除名称/实际出口在香港台湾的节点；只导入通过测试的去重配置，保留原池。

## 独立版 r10 · 2026-09-20

- 修复内置生图被本地 `unsupported_endpoint` 返回 404：放行 `/backend-api/codex/` 下的标准 HTTP 功能接口，统一映射到已配置上游，包含生图、改图、搜索及后续新增功能。
- 非聊天请求原样转发 multipart、二进制和压缩内容，保留转义路径与查询参数；不发探测、不注入主牌、不根据响应 state 删除主备。
- 图片渲染等待响应头最长 10 分钟，保持原节点拨号器，不修改聊天共用连接设置；功能接口自身的权限/额度错误不污染聊天账号状态。
- 请求记录区分图片生成、图片编辑和搜索；日志只记录操作类别、方法、状态和耗时，不记录提示词、图片、查询或凭据。

## 独立版 r9 · 2026-09-20

- 新采集返回 11 只暂停该模型的新采集，不按出口删除已有主备；正式响应只撤下本次实际使用且版本匹配的牌，迟到响应不能删除新主牌。
- 模型容量不足与普通流失败继续本轮下一节点；明确 401/403/429 仍按已有规则暂停，轮间间隔与总预算保留。
- AI 空闲默认 300 秒后停止补采；模型列表、面板与探测不延长计时，轮内逐个派发前再检查；在途 AI 回复保持活跃。
- 连接失败或探测读取超时将节点持久化为失败，自动轮询和固定节点均跳过；必须手动恢复，迟到的成功探测不能恢复它。
- 白色 iOS 风格页面，三模型固定顺序紧凑卡片，诊断折叠；节点搜索、筛选、批量勾选、卡片/列表切换，订阅导入识别 sub:// 并预览节点。
- 本地模拟上游、并发竞态和全包回归通过；不将这些测试当作真实上游成功率证明。

## 工作区修复 · 2026-09-19

- r8：后台及手动补采不再等待聊天结束，聊天到来不再中止当前节点轮询；长时间在途请求不再被视为空闲。保留单探测串行、轮间冷却、备用错峰、预算、上游暂停和请求 state/节点快照；新增探测开始与并发计数日志及持续回复场景回归。

- r7：节点状态改为独立 GET 快照，兼容旧 POST 读取；AI 回复和手动补采期间可查看、刷新、测速及停止测速。面板只禁用当前操作按钮，持续刷新状态且保留节点勾选；重建连接仍保护在途请求。

- 修复官方远程 compaction：`POST /responses/compact` 原样转发，不再改写成普通 `/responses`、追加 `compaction_trigger` 或重拼 SSE 响应；保留当前 state 出口绑定并继续跳过 state 注入。
- 增加原生压缩路径、上游失败正文保持和 gzip/zstd 请求的回归测试；验证通过后生成 `state-pool-20260919-r6` darwin-arm64 二进制。

## v0.4.0 · 2026-09-19

> 从 `codex/pool-upgrade` 升级分支交付。独立目录验收后发布，不覆盖 v0.3.1 标签或旧包。正式发布不代表所有真实上游已验证。

- 首页新增「一键检测环境配置」，只读检查本地状态，问题给出下一步入口；不调用模型、不读登录文件内容、不自动改配置，上游能力明确标为未验证。
- 新用户默认「拿到后固定使用」，可用期间不因提前刷新窗口反复采集；旧配置缺少策略字段时保留备用模式，首页可切换且不清空当前可用值。
- 不匹配正式响应正文不交给客户端；可用备用只提升给下一条请求，不自动重放可能已经计费的生成。
- 多订阅支持手动输入、粘贴与 TXT；明确追加或替换，保留来源检查、配置备份、来源/节点去重及数量限制。导入区默认勾选启用随机出口＋用后移出，追加时保留已选固定转发出口。
- 新增持久化节点生命周期：可用、已用、失败、停用；支持按清单全选、回收与可恢复清理，单节点有界采集仍受冷却和上游暂停限制。
- 高级设置可选择兼容出口、独立随机或用户固定；基础配置保持兼容，多订阅入口可在明确勾选下启用池策略，节点数量不等于独立公网 IP 数量。
- 请求体、zstd 窗口与压缩回复上限默认 64 MiB，可在 WebUI 调到 16–128 MiB；保留内存与并发边界，不提高上游限制。
- 中转助手规范常见地址、读取三模型列表，并安全补全缺失的 `base_url`；不覆盖已有地址，不保存临时检测 key，不对中转强制采集 292。
- 新增可选外部代理模式与合规边界说明；它是运行时约束，当前构建仍含内嵌核心，不冒称已完成精简合规发行版。
- 保留个人微信与 QQ 联系入口，新增朋友的 RedeemAI 卡网展示；保留真实贡献历史，不自动添加新的 AI 共同作者尾注。
- 真实中转 `/models` 返回 200，唯一 Sol 短生成请求返回 403 后停止；不宣称实际中转生成、Team 或全部真实场景已通过。

详细操作、兼容默认值和回退步骤见[v0.4.0 发布说明](docs/release-v0.4.0.md)。

## v0.3.1 · 2026-09-19

- 纳入 debin-ge 的 PR #2：修复 `[[skills.config]]` 等 TOML 数组表导致接入启动 panic，保留原贡献提交。
- WebUI 开放探测超时、采集间隔、本地有效期、提前刷新时间与每轮次数；沿用原有验证范围。
- 加入默认值、低频采集预设、撤销草稿、实时说明及保存确认；状态刷新不覆盖未保存时间。
- 修复订阅固定出口消失时，保存设置可能清空工作采集器却报告成功的问题。
- 微信群入口改为作者个人微信二维码，QQ 群保留。
- 新增时间配置 API 的校验、备份、外部修改保护、鉴权和限流保护测试。


## v0.1.0-alpha.1

首个公开测试版，目前只支持 Astra。

- 一个 Go 服务完成订阅导入、state 采集、当前/备用状态维护和 HTTP/SSE 转发。
- 自动备份并接管 Codex 配置；正常退出恢复，意外结束后可执行 `restore`。
- 支持 AnyTLS，以及 HTTP、HTTPS、SOCKS5、SS、SSR、VMess、VLESS、Trojan、Hysteria、Hysteria2、TUIC 出站。
- 订阅可指定下载客户端标识、选择协议、排除节点关键词；不关闭 TLS 校验。
- 修复首条请求等待备用采集、正式请求的认证/限流错误未统一暂停等问题。
- 默认 10 块规则下，从真实 AnyTLS 出口采到 292，连续两次 CLI 请求完成注入并收到回复。
- 提供 Windows x64/ARM64、macOS Apple Silicon/Intel 包、对应源码和 SHA256；附中文教程及群聊入口。

这是方法验证和早期使用版本，不保证改善降智或限流，也不会增加账号额度。Windows 桌面客户端完整联调、长期运行及回答质量对照仍待更多验证。详见 [测试记录](docs/testing.md)。

## v0.2.0-alpha.1

- 加入同进程、本机 Web 管理面板：注入开关、出口来源测试与应用、路由查看与固定、错误说明。
- 支持本地订阅文件和本机 HTTP 订阅，保留 HTTPS 远程订阅、HTTP / SOCKS5 等代理。
- 路由使用稳定匿名标识，订阅重排序不会静默把固定出口指到另一节点。
- 按选中的 Codex provider / profile 接管配置；区分 ChatGPT 登录、API Key、中转站，保留原认证设置。
- 兼容 CCS 常见的 API Key + `requires_openai_auth=true` 配置组合；中转默认不采集、不注入官方 state。
- 非正常退出后，核验旧事务并恢复；CCS 或用户改过配置时停止覆盖，保留原文件及备份。
- 区分本地 state 不可用和上游 503；切换注入或出口不能清除上游的限流、权限停止状态。
- 完善中文部署、面板操作与故障处理教程。
